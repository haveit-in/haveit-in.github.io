services:
  - type: static
    name: haveit-official
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: .
    routes:
      - type: rewrite
        source: /*
        destination: /index.html